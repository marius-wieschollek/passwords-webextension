import {AbstractModel} from 'passwords-client/models';
import Properties from '@js/Definition/DomForm';

export default class DomForm extends AbstractModel {

    constructor(data) {
        super(Properties, data);
    }

}